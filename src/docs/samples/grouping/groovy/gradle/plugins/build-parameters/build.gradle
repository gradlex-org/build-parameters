plugins {
    id 'org.gradlex.build-parameters' version '1.4.3'
}

// tag::grouping[]
buildParameters {
    group('myGroup') {
        description = "Optional description of the group"
        string('myString')
        integer('myInt')
    }
}
// end::grouping[]
